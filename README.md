The following concepts are used while building this project. 

DOM Manipulation: document.getElementById(), document.querySelector(), document.createElement("tr"), document.createElement("td"), innerHTML = '', innerText = char, appendChild(), classList.add("hideMe"), classList.remove("hideMe"), focus(), disabled = true.

Event Handling: addEventListener('click', ...), addEventListener("keyup", ...), addEventListener('keydown', ...), event.preventDefault(), event.key === 'Enter'.

Variable Declarations: const btn = ..., let puzzleString, let horizontal_L_R = [].

Arrays: horizontal_L_R.push(item), arr.forEach(element => ...), value.indexOf(item), [...element].reverse().

Strings: puzzleText.value.toUpperCase(), item += str[i], reverse().join(''), txt.toUpperCase(), item.search(txt).

Loops: for (let i=0; i<=str.length; i++), for (const char of element), for (const [key, value] of Object.entries(allLilnesObj)), for (const item of value).

Functions: function myButtonClicked(event), function createArray(str), nested function searchMe(txt), function getLineName(arrName).

Objects: allLilnesObj.horizontal_L_R = horizontal_L_R, Object.entries(allLilnesObj).

Control Structures: if (event.key === 'Enter'), if(str[i]=="," || i==str.length), if (txt.length>2), switch (arrName) { case ... }.

Spread Operator: [...element].reverse().
